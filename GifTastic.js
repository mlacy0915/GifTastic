var characters = ["Chucky Finster", "Hello Kitty", "Tweety", "Bart Simpson", "Stewie Griffin", "Jessica Rabbit", Angelica Pickles"]
        function renderButtons() {
            $("#buttons-view").empty();
            for (var i = 0; i < movies.length; i++) {
                var a = $("<button>");
            }
        }